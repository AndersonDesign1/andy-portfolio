import React from 'react';

const SvgIcon = (props) => (
  <svg
    xmlns="http://www.w3.org/2000/svg"
    viewBox="0 0 100 100" // Adjust the viewBox to match the Ahrefs SVG dimensions
    {...props}
  >
    {/* Paste the Ahrefs SVG content here */}
  </svg>
);

export default SvgIcon;
